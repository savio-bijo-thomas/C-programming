#include<stdio.h>
int main(){
  int num,f=1,temp;
  printf("Enter a number:");
  scanf("%d",&num);
  temp=num;
  while(num>0){
    f=f*num;
    num = num - 1;
  }
  printf("Factorial of %d is:%d",temp,f);
  return 0;

}
